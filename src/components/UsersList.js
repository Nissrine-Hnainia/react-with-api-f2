import axios from 'axios'
import React, {useState, useEffect} from 'react'
import { UserCard } from './UserCard'

const UsersList = () => {
    //make a state of users to update it
    

     //axios to send requests from the front
    

    //side effect: API 
    //empty array means the moment when the component mounts

    
    return (
        <div className="users">
            <h2> The list of users</h2>
            <div className="userList">
            {/*map the users list*/}
        </div>
        </div>
    )
}

export default UsersList
